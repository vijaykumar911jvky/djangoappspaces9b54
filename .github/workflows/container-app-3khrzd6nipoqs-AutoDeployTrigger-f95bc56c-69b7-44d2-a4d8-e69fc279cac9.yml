name: Trigger auto deployment for container-app-3khrzd6nipoqs

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-3khrzd6nipoqs-AutoDeployTrigger-f95bc56c-69b7-44d2-a4d8-e69fc279cac9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP3KHRZD6NIPOQS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP3KHRZD6NIPOQS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP3KHRZD6NIPOQS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP3KHRZD6NIPOQS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP3KHRZD6NIPOQS_REGISTRY_PASSWORD }}
          containerAppName: container-app-3khrzd6nipoqs
          resourceGroup: djangoappspaces9b54ad41
          imageToBuild: default/container-app-3khrzd6nipoqs:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


